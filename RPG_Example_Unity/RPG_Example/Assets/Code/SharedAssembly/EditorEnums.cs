public static class EditorEnums
{
    public enum WindowKind { None, Damage, Vfx, Combo, DodgeCancel, MoveCancel, MoveRotation, IFrame, AttackBuffer, BufferEnd }
    public enum TimelineDragMode { None, Scrub, Marker, Move, ResizeL, ResizeR }
}
